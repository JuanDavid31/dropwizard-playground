logging:
  level: INFO
  loggers:
    org.mobile: DEBUG

database:
  driverClass: org.h2.Driver
  user: sa
  password:
  url: jdbc:h2:./db/test

server:
  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0